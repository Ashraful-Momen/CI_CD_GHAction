Perfect 👍 you already know CI, so let’s **focus only on real-world CD (Continuous Deployment) with Git** — in a way that matches your stack
(Laravel + Docker + Nginx + Swarm / VM / Linux).

I’ll show you the **production-style CD flow** used by DevOps teams.

---

# 1️⃣ What CD Really Means (for you)

> **CD = when you push to Git → server updates automatically**

No FTP, no manual SSH deploy.

For you, CD will mean:

```
git push main
      ↓
GitHub Actions / GitLab CI
      ↓
Build + Test
      ↓
SSH into server
      ↓
docker stack deploy
      ↓
Zero-downtime update
```

---

# 2️⃣ Your CD Architecture (Laravel + Docker Swarm)

```
Local PC
   |
   | git push
   ↓
GitHub Repo
   |
   | GitHub Actions (runner)
   ↓
SSH → Production VM
   |
   | docker stack deploy
   ↓
Nginx → Laravel containers
```

---

# 3️⃣ Server One-Time Setup

On your **production VM**:

```bash
# Install docker & swarm
curl -fsSL https://get.docker.com | sh
docker swarm init

# Allow ssh login for deploy user
adduser deploy
usermod -aG docker deploy
```

Create project folder:

```bash
mkdir -p /var/www/lindos
cd /var/www/lindos
git clone https://github.com/your/repo.git .
```

---

# 4️⃣ Add SSH Key to GitHub

On your local:

```bash
ssh-keygen -t ed25519 -C "cd-deploy"
cat ~/.ssh/id_ed25519.pub
```

Add it to:

> GitHub → Settings → Secrets → New secret

| Name              | Value                 |
| ----------------- | --------------------- |
| `SSH_PRIVATE_KEY` | content of id_ed25519 |
| `SERVER_IP`       | your VM IP            |
| `SERVER_USER`     | deploy                |

---

# 5️⃣ GitHub Actions CD Pipeline

Create:

```
.github/workflows/cd.yml
```

```yaml
name: CD Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

    - name: Deploy to server
      run: |
        ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} << 'EOF'
        cd /var/www/lindos
        git pull
        docker stack deploy -c docker-compose.yml lindos
        EOF
```

Now every push to `main` **auto-deploys**.

---

# 6️⃣ Zero Downtime (Swarm Rolling Update)

In `docker-compose.yml`:

```yaml
services:
  app:
    image: lindos_app
    deploy:
      replicas: 3
      update_config:
        parallelism: 1
        delay: 10s
```

Swarm will replace containers **one by one** → no downtime.

---

# 7️⃣ Rollback

```bash
docker service ls
docker service ps lindos_app
docker service update --rollback lindos_app
```

---

# 8️⃣ Your Learning Roadmap (Only CD)

| Day | Topic                        |
| --- | ---------------------------- |
| 1   | SSH, Secrets, GitHub Actions |
| 2   | Swarm deploy + rollback      |
| 3   | Zero downtime updates        |
| 4   | Tag-based release (v1.2.0)   |
| 5   | Canary deployment            |
| 6   | Logs + healthcheck           |
| 7   | Monitoring & alert           |

---

If you want, tell me:

* GitHub or GitLab?
* Single server or cluster?
* Docker Swarm or plain Docker?

I’ll give you a **copy-paste production CD pipeline** for your exact setup 🔥
