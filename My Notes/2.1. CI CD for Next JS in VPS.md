# Next.js Complete VPS CI/CD Note (GitHub Actions + Docker + Docker Compose + Docker Swarm)

# 1. Real Production CI/CD Architecture

```txt id="jlwm01"
DEVELOPER
    │
    ▼
GIT PUSH
    │
    ▼
GITHUB ACTIONS
    │
    ├── INSTALL DEPENDENCY
    ├── RUN TEST
    ├── BUILD DOCKER IMAGE
    ├── PUSH IMAGE
    │
    ▼
DOCKER REGISTRY
(DockerHub/GHCR)
    │
    ▼
VPS SERVER
    │
    ├── PULL IMAGE
    ├── RESTART CONTAINER
    └── DEPLOY NEW VERSION
    │
    ▼
LIVE APPLICATION
```

---

# 2. Real Production Stack

| Tool           | Purpose                    |
| -------------- | -------------------------- |
| GitHub Actions | CI/CD Automation           |
| Docker         | Container                  |
| Docker Compose | Multi Container            |
| Docker Swarm   | Multi Server Orchestration |
| VPS            | Production Server          |
| NGINX          | Reverse Proxy              |
| DockerHub/GHCR | Docker Registry            |

---

# 3. Full Stack Next.js Architecture

```txt id="jlwm02"
NEXT.JS FRONTEND
        │
        ▼
NEXT.JS API / BACKEND
        │
        ▼
REDIS / RABBITMQ
        │
        ▼
POSTGRES / MYSQL
```

---

# 4. CI/CD Main Steps

```txt id="jlwm03"
1. PUSH CODE
2. RUN TEST
3. BUILD IMAGE
4. PUSH IMAGE
5. SSH INTO VPS
6. PULL NEW IMAGE
7. RESTART CONTAINER
8. LIVE DEPLOY
```

---

# 5. VPS Required Setup

Install:

```bash id="jlwm04"
docker
docker compose
nginx
git
```

Optional:

```bash id="jlwm05"
docker swarm
portainer
```

---

# 6. VPS Folder Structure

```txt id="jlwm06"
/var/www/myapp/
│
├── docker-compose.yml
├── .env
├── nginx/
│   └── default.conf
└── data/
```

---

# 7. Dockerfile for Next.js

## Dockerfile

```dockerfile id="jlwm07"
FROM node:20

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

---

# 8. Build Docker Image

```bash id="jlwm08"
docker build -t myapp .
```

---

# 9. Run Docker Container

```bash id="jlwm09"
docker run -p 3000:3000 myapp
```

---

# 10. Docker Compose Architecture

```txt id="jlwm10"
DOCKER COMPOSE
    │
    ├── NEXT.JS
    ├── MYSQL
    ├── REDIS
    └── NGINX
```

---

# 11. Docker Compose Example

## docker-compose.yml

```yaml id="jlwm11"
version: "3.9"

services:

    app:

        build: .

        container_name: next-app

        ports:
            - "3000:3000"

        env_file:
            - .env

        restart: always


    redis:

        image: redis:latest

        restart: always
```

---

# 12. Run Docker Compose

```bash id="’wini12"
docker compose up -d
```

---

# 13. GitHub Actions CI/CD Architecture

```txt id="’wini13"
GITHUB ACTIONS
    │
    ├── TEST
    ├── BUILD
    ├── PUSH IMAGE
    └── DEPLOY VPS
```

---

# 14. GitHub Secrets

Set inside:

```txt id="’wini14"
GitHub Repo
    │
    ▼
Settings
    │
    ▼
Secrets and Variables
```

---

# 15. Important Secrets

| Secret          | Purpose            |
| --------------- | ------------------ |
| VPS_HOST        | VPS IP             |
| VPS_USER        | VPS Username       |
| VPS_SSH_KEY     | SSH Private Key    |
| DOCKER_USERNAME | DockerHub Username |
| DOCKER_PASSWORD | DockerHub Password |

---

# 16. Complete CI/CD Workflow

## .github/workflows/deploy.yml

```yaml id="’wini15"
name: Production Deploy

on:

    push:

        branches:
            - main

jobs:

    deploy:

        runs-on: ubuntu-latest

        steps:

            - name: Checkout Code

              uses: actions/checkout@v4


            - name: Setup Node.js

              uses: actions/setup-node@v4

              with:
                  node-version: "20"


            - name: Install Dependencies

              run: npm install


            - name: Run Test

              run: npm test


            - name: Build Next.js

              run: npm run build


            - name: Docker Login

              run: |

                  docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}


            - name: Build Docker Image

              run: |

                  docker build -t mydocker/myapp:latest .


            - name: Push Docker Image

              run: |

                  docker push mydocker/myapp:latest


            - name: Deploy VPS

              uses: appleboy/ssh-action@v1.0.3

              with:

                  host: ${{ secrets.VPS_HOST }}

                  username: ${{ secrets.VPS_USER }}

                  key: ${{ secrets.VPS_SSH_KEY }}

                  script: |

                      cd /var/www/myapp

                      docker compose pull

                      docker compose down

                      docker compose up -d
```

---

# 17. VPS Deployment Flow

```txt id="’wini16"
VPS
 │
 ├── docker compose pull
 ├── stop old container
 ├── start new container
 └── app live
```

---

# 18. Production Docker Compose

## docker-compose.yml

```yaml id="’wini17"
version: "3.9"

services:

    nextjs:

        image: mydocker/myapp:latest

        container_name: nextjs-app

        restart: always

        ports:
            - "3000:3000"

        env_file:
            - .env
```

---

# 19. NGINX Reverse Proxy

## nginx/default.conf

```nginx id="’wini18"
server {

    listen 80;

    server_name example.com;

    location / {

        proxy_pass http://localhost:3000;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

# 20. Restart NGINX

```bash id="’wini19"
sudo systemctl restart nginx
```

---

# 21. SSL Setup

Use:

```bash id="’wini20"
sudo certbot --nginx
```

---

# 22. Docker Swarm Concept

Docker Swarm = Multi Server Container Management

---

# 23. Swarm Architecture

```txt id="’wini21"
MANAGER NODE
      │
      ├── WORKER NODE 1
      ├── WORKER NODE 2
      └── WORKER NODE 3
```

---

# 24. Initialize Docker Swarm

```bash id="’wini22"
docker swarm init
```

---

# 25. Deploy Swarm Service

```bash id="’wini23"
docker service create \
--name nextjs-app \
-p 3000:3000 \
mydocker/myapp:latest
```

---

# 26. Docker Stack Deploy

## stack.yml

```yaml id="’wini24"
version: "3.9"

services:

    nextjs:

        image: mydocker/myapp:latest

        ports:
            - "3000:3000"

        deploy:

            replicas: 3

            restart_policy:

                condition: any
```

---

# 27. Deploy Stack

```bash id="’wini25"
docker stack deploy -c stack.yml myapp
```

---

# 28. Swarm Deployment Flow

```txt id="’wini26"
PUSH CODE
    │
    ▼
GITHUB ACTIONS
    │
    ▼
BUILD IMAGE
    │
    ▼
PUSH IMAGE
    │
    ▼
SWARM MANAGER
    │
    ▼
UPDATE ALL NODES
```

---

# 29. Zero Downtime Deployment

Docker Swarm supports:

```txt id="’wini27"
ROLLING UPDATE
```

Users do not see downtime.

---

# 30. Production Best Practice

| Best Practice          | Purpose                |
| ---------------------- | ---------------------- |
| Run Test Before Deploy | Prevent Bad Deploy     |
| Use Docker Image       | Consistent Environment |
| Use Secrets            | Secure Credentials     |
| Use NGINX              | Reverse Proxy          |
| Use SSL                | HTTPS                  |
| Use Swarm              | High Availability      |
| Use Health Check       | Stable Service         |

---

# 31. Health Check Example

```yaml id="’wini28"
healthcheck:

    test: ["CMD", "curl", "-f", "http://localhost:3000"]

    interval: 30s

    timeout: 10s

    retries: 3
```

---

# 32. Complete Production Flow

```txt id="’wini29"
LOCAL DEVELOPMENT
        │
        ▼
GIT PUSH
        │
        ▼
GITHUB ACTIONS
        │
        ├── INSTALL
        ├── TEST
        ├── BUILD
        ├── DOCKER BUILD
        ├── PUSH IMAGE
        │
        ▼
VPS SERVER
        │
        ├── PULL IMAGE
        ├── RESTART CONTAINER
        ├── NGINX PROXY
        └── SSL ENABLE
        │
        ▼
LIVE APPLICATION
```

---

# 33. Real Production Commands

## Check Container

```bash id="’wini30"
docker ps
```

---

## Check Logs

```bash id="’wini31"
docker logs container_name
```

---

## Restart Container

```bash id="’wini32"
docker restart container_name
```

---

## Stop Compose

```bash id="’wini33"
docker compose down
```

---

## Start Compose

```bash id="’wini34"
docker compose up -d
```

---

# 34. Common Production Errors

| Error               | Solution          |
| ------------------- | ----------------- |
| Port already used   | Change port       |
| Docker build failed | Check Dockerfile  |
| SSH failed          | Check VPS SSH Key |
| NGINX 502           | App not running   |
| Container crash     | Check logs        |

---

# 35. Important Production Rules

## RULE 1

```txt id="’wini35"
NEVER STORE SECRET INSIDE GIT
```

---

## RULE 2

```txt id="’wini36"
RUN TEST BEFORE DEPLOY
```

---

## RULE 3

```txt id="’wini37"
USE DOCKER FOR CONSISTENT ENVIRONMENT
```

---

## RULE 4

```txt id="’wini38"
USE NGINX AS REVERSE PROXY
```

---

## RULE 5

```txt id="’wini39"
USE HTTPS IN PRODUCTION
```

---

# 36. Interview Questions

## Q1: Why use Docker in CI/CD ?

Same environment everywhere.

---

## Q2: Why use Docker Compose ?

Manage multiple containers together.

---

## Q3: Why use Docker Swarm ?

Scaling and high availability.

---

## Q4: Why use NGINX ?

Reverse proxy and load balancing.

---

## Q5: Why use GitHub Secrets ?

Secure credential management.

---

# 37. Final Quick Summary

| Topic          | Meaning                |
| -------------- | ---------------------- |
| CI             | Continuous Integration |
| CD             | Continuous Deployment  |
| Docker         | Container              |
| Compose        | Multi Container        |
| Swarm          | Multi Server           |
| VPS            | Production Server      |
| NGINX          | Reverse Proxy          |
| GitHub Actions | Automation             |
| Registry       | Store Docker Image     |
| Secrets        | Secure Credentials     |
